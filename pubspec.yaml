name: projects_gallery
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_icons:
  google_fonts:
  smooth_star_rating:
  syncfusion_flutter_gauges: ^18.3.47

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/coffee_shop/
    - assets/images/vpn_ui/premiumcrown.png
